# Materials

按人物放材料。公开仓库不附带真实材料，请只放你自己拥有使用权、或确认可以合法处理的文件。

```text
materials/汪曾祺/sources/
materials/费孝通/sources/
```

支持：`.txt`、`.md`、`.org`、`.pdf`、`.epub`、`.docx` 等。DOCX 需要可选依赖。

`sources/` 下的内容会被本地索引器读取，并在 `indexes/roundtable.sqlite` 中生成派生文本和 embedding。材料和索引都不应提交到公开仓库。
